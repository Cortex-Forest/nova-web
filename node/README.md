# Nova Web — Node 模块（预留）

本目录预留 Node Dashboard 数据层，`types.ts` 定义了节点与奖励的类型契约。

## 未来接入方案

1. 节点客户端暴露本地管理 API（与公共 RPC 分离，仅本地/授权访问）。
2. `node/` 下实现 `client.ts`：轮询/流式获取节点状态。
3. `/node` 页面接入 Dashboard，替换当前的占位预览。

## 原则

- 指标必须真实来自节点，不可用占位数据冒充。
- 奖励未启用时显示 null / 明确文案，不显示假收益。
